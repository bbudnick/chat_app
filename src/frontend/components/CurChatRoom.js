/*
    CurChatRoom is the current chat room with the title, 
    chat history, input bar for chat submission, and option to 
    share and recieve files. 
*/

import React from 'react';
import '../stylesheets/style.css';
import Logo from './logo.png';

export function CurChatRoom() {
    return (
        <div>Empty</div>
    );
};